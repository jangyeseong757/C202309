#include <stdio.h>

int main()
	{
		int x;
		int y;
		int z;
		// x y z라는 변수 설정

		x = 1;
		y = 2;
		z = x + y;
		//x에는 1이라는 값을 y에는 2라는 값을 
		//z에는 x와 y를 더한값을 넣는다

		printf("%d", z);
		//z를 출력한다

		return 0;
	}

